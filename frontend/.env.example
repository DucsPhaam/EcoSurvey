# When running frontend in dev with `npm run dev`, Vite proxies /api → http://localhost:5000.
# Keep this blank OR set to http://localhost:5000/api (both work in dev).
# In Docker, the frontend build sets VITE_API_URL=/api automatically.
VITE_API_URL=
